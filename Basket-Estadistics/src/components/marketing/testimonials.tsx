// Testimonials section removed in bug-bash-v1 (B7).
// The prompt 1 worktree is responsible for rebuilding this surface
// once we have real, named beta users to quote. Until then we keep
// a placeholder constant so the marketing page import does not break.
export const TESTIMONIALS_REMOVED = true
